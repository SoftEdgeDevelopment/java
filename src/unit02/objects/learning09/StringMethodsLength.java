package unit02.objects.learning09;

public class StringMethodsLength {
    public static void main(String[] args) {
        String str1 = "Programming";
     //another way to write as a new variable   int lengthOfStr = str1.length();
     //dispaly the value of the new variable   System.out.println(lengthOfStr);
        //this will print out the amount of characters used in the length of the string
        System.out.println(str1.length());

    }
}
